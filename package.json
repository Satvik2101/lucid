{
    "devDependencies": {
        "@faker-js/faker": "^8.4.1",
        "@types/jest": "^29.5.12",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.2"
    },
    "scripts": {
        "test": "jest --testPathIgnorePatterns=/build/*",
        "generate": "ts-node gen.ts",
        "gen-test": "npm run generate && npm run test"
    },
    "version": "0.0.1"
}